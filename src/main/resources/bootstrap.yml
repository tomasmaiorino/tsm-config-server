# configure eureka client
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka
    healthcheck:
      enabled: true
spring:
  application:
    name: config-service
  cloud:
    config:
      discovery:
        enabled: true      
